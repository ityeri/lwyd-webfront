# Build-time variable. Leave unset for same-origin proxying via nginx (/api).
# Set only when the frontend is served from a different origin than the API.
# VITE_API_BASE_URL=http://localhost:8000/api
